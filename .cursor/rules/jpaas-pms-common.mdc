---
description: jpaas-pms 前后端通用开发约定（协作/一致性/质量门禁）
alwaysApply: true
---

# 前后端通用开发规范（jpaas-pms-common）

## 目标

- 保证前后端接口、字段、类型与业务语义保持一致，减少联调与回归问题。
- 改动优先最小化且可追溯：只改任务相关内容，避免无关重构。

## 接口与字段一致性

- 新增或修改接口时，优先复用既有字段命名与返回结构；不要在同一业务中混用多套命名风格。
- 涉及主键/外键（如 `Id`）时，明确前后端序列化约定并在代码中保持一致，避免精度或类型歧义。
- 任何会影响前后端契约的变更（字段增删改、必填性、枚举值）必须同步更新对应类型/DTO/文档。

## 代码改动原则

- 遵循“就近修改”：优先沿用当前模块既有分层、目录与命名，不新增平行技术方案。
- 逻辑改动与重命名/格式化分离，避免在一次提交中掺杂大量无关噪音。
- 禁止提交调试残留（临时日志、注释掉的大段代码、无用导入、占位实现）。

## 质量门禁

- 变更完成后至少通过与改动相关的编译、类型检查、lint 或测试，确保没有明显回归风险。
- 发现上下游影响（接口调用方、数据库脚本、配置项）时，补齐必要联动改动后再提交。
- 若存在无法本地验证的风险点，在提交说明中明确影响范围、前置条件与验证建议。

## 文档与提交说明

- 对外行为变化（接口字段、业务流程、配置开关）需补充简要说明，确保他人可快速理解变更原因。
- commit message 使用中文，聚焦“改动目的 + 影响范围”，避免空泛描述。
